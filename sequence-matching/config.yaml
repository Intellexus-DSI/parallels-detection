algorithm:
  match_score: 1.0
  mismatch_score: -1.5
  open_gap_score: -1.0
  extend_gap_score: -1.0
  min_score: 20.0
  max_iterations: 100

seeding:
  k: 40
  max_gap: 20
  extend: 20
  max_kmer_hits: 10

preprocessing:
  strip_chars: "\\_"

input:
  dir: "input"

output:
  path: "output/results.csv"