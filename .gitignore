# Virtual environments
.venv/
*.venv/
venv/

# IDE
.claude/

# Generated config (from run_pipeline)
enriching/.enriching_config_generated.yaml

# Notebooks
playground.ipynb

# Output files (keep directories with .gitkeep)
output/*
!output/.gitkeep
# output/*.json
# output/*.parquet
segmentation/output/
# embedding/output/
detection/output/

# Sequence matching — generated outputs and regen corpora
sequence-matching/output/
sequence-matching/**/corpus_derge/
sequence-matching/**/corpus_ab/
sequence-matching/.pytest_cache/
sequence-matching/data/canon/

# Word lock files
~$*

# Data files
data/*.jsonl
data/*.json

# Large corpus files (exceed GitHub 100MB limit)
derge-kangyur.jsonl
derge-tengyur.jsonl
derge-both.jsonl

# Segmentation output (can be large)
data/segmented_output/
# *.npy
# *.xlsx
**/Single_Lines/*

# Python
__pycache__/
*.pyc
*.egg-info/
data/05_clean_data/

# Submodule (tracked separately)
# detect_and_convert/ is a submodule, don't ignore it
