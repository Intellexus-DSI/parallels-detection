# Test configuration with matching row counts
segments_csv: "data/test_segments.csv"
embeddings_path: "data/embeddings.npy"
output_path: "output/test_parallels.csv"

matching:
  strategy: "threshold"
  threshold: 0.85

processing:
  batch_size: 1000
  normalize_embeddings: true

output:
  format: "csv"
  include_text: true
