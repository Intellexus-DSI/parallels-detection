# Use faiss-cpu for CPU-only, or faiss-gpu for GPU support (requires CUDA)
# Install only ONE of these:
faiss-cpu>=1.7.0
# faiss-gpu>=1.7.0  # Uncomment for GPU support (and comment out faiss-cpu)
numpy>=1.21.0
pandas>=1.3.0
pydantic>=2.0.0
pyyaml>=6.0
tqdm>=4.60.0
pytest>=7.0.0
openpyxl>=3.0.0
